# DEEPEN-LOG — MiniTVAN

- 2026-06-12 | feat: Xử lý hóa đơn sai sót — Điều chỉnh (tăng/giảm) & Thay thế hóa đơn đã phát hành. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Save_Adj`, `Invoice_Invoice_Save_Replace`, `Invoice_Invoice_SaveX`; hằng số `TConst.SourceInvoiceCode` INVOICEROOT/INVOICEREPLACE/INVOICEADJ và `TConst.InvoiceAdjType` ADJINCREASE/ADJDESCREASE/NORMAL). Thêm enum `SourceInvoiceCode`/`InvoiceAdjType`, trường `RefInvoiceId`/`RefTctCode`/`AdjReason` trên `Invoice`, service `AdjustAsync`/`ReplaceAsync` (HĐ gốc phải Accepted; HĐ đã điều chỉnh không điều chỉnh tiếp; thay thế hủy HĐ gốc), endpoint minimal-API `/api/invoices/{id}/adjust|replace`, action + form Razor ở `Views/Invoice/Detail.cshtml`, seed 1 HĐ điều chỉnh giảm, 5 test mới. Build Release 0 error, 12/12 test pass.
- 2026-06-12 | feat: Quản lý hạn mức hóa đơn (Invoice_license) — cấp/tăng hạn mức phát hành theo NNT + lịch sử cấp/điều chỉnh. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_license_IncreaseQty`/`Invoice_license_IncreaseQtyX` — nếu MST chưa có trong `Invoice_license` thì insert TotalQty=0 rồi cộng Qty; `Invoice_license_Get`; `Invoice_licenseCreHist_Get`; bảng `Invoice_license`/`Invoice_licenseCreHist`). Trước đó entity `InvoiceLicense`/`LicenseHist` + enum `LicenseHistType` đã có sẵn trong Models/DbContext nhưng CHƯA dùng (không service/controller/view/endpoint/seed). Thêm service `LicensesAsync`/`GetLicenseAsync`/`IncreaseLicenseAsync` (tạo mới nếu chưa có, cộng dồn TotalQty, ghi `LicenseHist` với Type Create/Increase, chặn Qty<=0 và NNT không tồn tại)/`LicenseHistsAsync`, `LicenseController` + `Views/License/Index.cshtml` (bảng hạn mức + progress còn lại + lịch sử + form cấp), helper `Ui.LicHist`, nav link, endpoint minimal-API `/api/licenses` (GET) và `/api/licenses/increase` (POST), seed 1 hạn mức 1000 cho NNT demo, 4 test mới. Build Release 0 error, 16/16 test pass.
- 2026-06-12 | feat: Bảng tổng hợp dữ liệu hóa đơn điện tử gửi CQT (Mst_GuiTongHop) — NNT lập bảng tổng hợp theo kỳ (ngày/tháng/quý/năm) gom các HĐ đã được CQT chấp nhận rồi gửi tới cơ quan thuế. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/InvoiceHub.cs`: `Mst_GuiTongHop_Create`/`Mst_GuiTongHop_CreateX`/`Mst_GuiTongHop_Get`/`Mst_GuiTongHop_GetX`; model `idN.TVAN.Common/Models/TDiepTCT/Mst_GuiTongHop.cs` + `Mst_GuiTongHopDtl.cs` + `Mst_GuiTongHopInfo.cs`; hằng số `TblMst_GuiTongHop`/`TblMst_GuiTongHopInfo` trong `Const.Main.cs`; ràng buộc `Mst_GuiTongHop_Create_Invalid_BSLThu_And_LDau` — BSLThu!=0 thì LDau=0, ngược lại LDau=1). Thêm enum `PeriodType` (LKDLieu) + `GthStatus`, entity `GuiTongHop`/`GuiTongHopDtl`, service `GuiTongHopsAsync`/`GetGuiTongHopAsync`/`CreateGuiTongHopAsync` (gom HĐ Accepted theo khoảng kỳ, chặn kỳ rỗng)/`SendGuiTongHopAsync` (round-trip 202/204), `GuiTongHopController` + `Views/GuiTongHop/Index.cshtml` + `Detail.cshtml`, helper `Ui.Period`/`Ui.Gth`, nav link, endpoint minimal-API `/api/guitonghop` (GET/POST) và `/api/guitonghop/{id}/send`, seed 1 bảng tổng hợp demo, 4 test mới. Build Release 0 error, 20/20 test pass.
- 2026-06-12 | feat: Tra cứu thông tin người nộp thuế theo MST từ cơ quan thuế (TCT_TraTTinMaSoThue) — nhập MST, hệ thống gửi yêu cầu tới CQT và trả về tên NNT, địa chỉ, cơ quan thuế quản lý; mọi lần tra cứu ghi nhật ký đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Web/Controllers/LoadDataController.cs`: `LoadTCT_TraTTinMaSoThue` — dựng XML `TRUYVAN.MST`, ký số, gọi `TCT_TraTTinMaSoThueService.WA_T2B_TCT_TraTTinMaSoThue_BaseHTTPReq`, lấy `Lst_Mst_NNT[0]` rồi tra `Mst_GovTaxID` để lấy `GovTaxName`; model `idN.TVAN.Common/Models/TCT_TraTTinMaSoThue.cs` + `RQ_/RT_TCT_TraTTinMaSoThue.cs` + `Mst_GovTaxID.cs`; bảng `Mst_GovTaxID` trong `Const.Main.cs`). Thêm enum `LookupResult` (Success/NotFound/Error), entity `TaxOffice` (danh mục CQT theo GovTaxID) + `NntLookupLog` (nhật ký tra cứu), service `TaxOfficesAsync`/`NntLookupLogsAsync`/`LookupNntByMstAsync` (MST 10/13/14 số → tìm thấy, ngược lại báo không tìm thấy; luôn ghi log), `TaxLookupController` + `Views/TaxLookup/Index.cshtml` (form tra cứu + kết quả + lịch sử + danh mục CQT), helper `Ui.Lookup`, nav link "Tra cứu MST", endpoint minimal-API `/api/tax/lookup/{mst}` (GET) và `/api/tax/offices` (GET), seed 2 cơ quan thuế + 1 nhật ký tra cứu demo, 4 test mới. Build Release 0 error, 23/23 test pass.
- 2026-06-12 | feat: Gửi/gửi lại email hóa đơn đã phát hành cho người mua (Invoice_Invoice_Support_SendMail) — chỉ gửi được cho HĐ đã được CQT chấp nhận (Accepted), người nhận lấy từ tham số hoặc EmailSend đã lưu, mọi lần gửi ghi nhật ký đối soát và cập nhật thời điểm/người gửi trên HĐ. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_SendMail`/`Invoice_Invoice_Support_SendMailX` — kiểm tra `InvoiceStatus == Issued` mới gửi, lấy `EmailSend` (nhiều địa chỉ cách nhau ';') fallback `CustomerNNTEmail`, gọi `Biz_SendMail` rồi update `SendEmailDTimeUTC`/`SendEmailBy`; `idN.TVAN.Biz/InvoiceHub.cs`: `Biz_SendMail`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `EmailSend`/`SendEmailDTimeUTC`/`SendEmailBy`/`CustomerNNTEmail`). Thêm enum `EmailSendResult` (Success/Failed), trường `EmailSend`/`SendEmailDTimeUTC`/`SendEmailBy` trên `Invoice`, entity `InvoiceEmailLog` (nhật ký gửi email), service `SendInvoiceEmailAsync` (chặn HĐ chưa Accepted, chặn email rỗng/không hợp lệ, tách nhiều địa chỉ theo ';')/`EmailLogsAsync`, `EmailLogController` + `Views/EmailLog/Index.cshtml` (bảng nhật ký + lọc theo HĐ), form "Gửi email hóa đơn" + bảng nhật ký email ở `Views/Invoice/Detail.cshtml`, helper `Ui.Email`, nav link "Nhật ký gửi email", endpoint minimal-API `/api/invoices/{id}/send-email` (POST) và `/api/email-logs` (GET), seed 1 nhật ký gửi email demo, 4 test mới. Build Release 0 error, 27/27 test pass.
- 2026-06-12 | feat: Chuyển hóa đơn về trạng thái chờ (PENDING) — giữ nguyên số hóa đơn. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_InvoiceToPending` — update `InvoiceStatus='PENDING'`, xóa `ApprDTimeUTC/ApprBy/IssuedDTimeUTC/IssuedBy/InvoiceFilePath/InvoicePDFFilePath/MailSentDTimeUTC/SendEmailDTimeUTC/SignedDate/SignBy`; hằng số `TConst.InvoiceStatus.Pending` trong `Const.Main.BE.cs`). Thêm service `ResetToPendingAsync` (chặn HĐ đang Draft hoặc đang Sent; đưa HĐ Accepted/Rejected/Cancelled về Draft, xóa `TctCode`/`RejectReason`/`SentAt`/`EmailSend`/`SendEmailDTimeUTC`/`SendEmailBy`, giữ nguyên `No`, ghi `TranMessage` nhật ký), endpoint minimal-API `/api/invoices/{id}/to-pending` (POST), action `InvoiceController.ToPending` + form "Chuyển về trạng thái chờ" ở `Views/Invoice/Detail.cshtml`, 4 test mới. Build Release 0 error, 31/31 test pass.
- 2026-06-12 | feat: Khôi phục hóa đơn đã hủy (DELETED → ISSUED) để làm thông báo sai sót — chuyển HĐ đã hủy về lại trạng thái đã phát hành, giữ nguyên mã tra cứu CQT. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_BackInvoiceStatus`/`Invoice_Invoice_Support_BackInvoiceStatusX` — chỉ nhận HĐ ở trạng thái `ISSUED`/`DELETED`, giữ `InvoiceVerifyCQTCode` nếu đầu vào không truyền mã mới, update `InvoiceStatus`; hằng số `TConst.InvoiceStatus.Issued`/`Deleted` trong `Const.Main.BE.cs`). Thêm service `RestoreInvoiceAsync` (chặn HĐ không phải Cancelled, chặn HĐ thiếu mã tra cứu; đưa Cancelled → Accepted, xóa RejectReason, ghi `TranMessage` nhật ký), endpoint minimal-API `/api/invoices/{id}/restore` (POST), action `InvoiceController.Restore` + form "Khôi phục về đã phát hành" ở `Views/Invoice/Detail.cshtml` (chỉ hiện khi HĐ đã hủy và còn mã tra cứu), 3 test mới. Build Release 0 error, 34/34 test pass.
- 2026-06-12 | feat: Xóa hóa đơn điều chỉnh/thay thế CHƯA phát hành (Invoice_Invoice_Support_DeleteInvoiceRefNo) — hủy bỏ một HĐ điều chỉnh/thay thế lập sai trước khi truyền tới CQT. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_DeleteInvoiceRefNo`/`Invoice_Invoice_Support_DeleteInvoiceRefNoX` — kiểm tra HĐ tồn tại, `InvoiceStatus == Pending`, `RefNo` không rỗng, `InvoiceNo` rỗng, `SourceInvoiceCode` phải là `INVOICEADJ`/`INVOICEREPLACE`; sau đó DELETE `Invoice_Invoice` + `Invoice_InvoiceDtl`; hằng số `TConst.SourceInvoiceCode.InvoiceADJ`/`InvoiceReplace` và `TConst.InvoiceStatus.Pending` trong `Const.Main.BE.cs`). Thêm service `DeleteAdjustReplaceAsync` (chặn HĐ không phải Draft, chặn HĐ không phải điều chỉnh/thay thế, chặn HĐ thiếu tham chiếu gốc `RefTctCode`, chặn HĐ đã có mã tra cứu `TctCode`; xóa HĐ + ghi `TranMessage` nhật ký), endpoint minimal-API `/api/invoices/{id}/delete-adjust-replace` (POST), action `InvoiceController.DeleteAdjustReplace` + form "Xóa HĐ điều chỉnh/thay thế chưa phát hành" ở `Views/Invoice/Detail.cshtml` (chỉ hiện khi HĐ Draft/Rejected và là HĐ điều chỉnh/thay thế có tham chiếu gốc), 4 test mới. Build Release 0 error, 38/38 test pass.
- 2026-06-12 | feat: In chuyển đổi hóa đơn (cờ FlagChange) — đánh dấu HĐ đã in ở dạng chuyển đổi và bỏ cờ để in lại HĐ thường. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_BackFlagChange`/`Invoice_Invoice_Support_BackFlagChangeX` — update `Invoice_Invoice.FlagChange='1'` (chưa in chuyển đổi) cho danh sách InvoiceCode; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `FlagChange` — "trạng thái hóa đơn in chuyển đổi (1 là chưa in, 0 là đã in)"; hằng số `TConst.Flag.Active`/`Inactive` trong `Const.Main.BE.cs`; luồng view in chuyển đổi ở `idN.TVAN.Web/Controllers/Invoice_InvoiceController.cs` — `inchuyendoi="1"` → `FlagChange="0"`). Thêm enum `ConversionPrintFlag` (NotPrinted=1/Printed=0) + `ConversionPrintAction` (Print/Reset), trường `FlagChange` trên `Invoice`, entity `ConversionPrintLog` (nhật ký in/bỏ cờ), service `MarkConversionPrintedAsync` (chặn HĐ chưa Accepted, chặn in lại khi đã in)/`ResetConversionPrintAsync` (chặn khi chưa in)/`ConversionPrintLogsAsync`, `ConversionPrintLogController` + `Views/ConversionPrintLog/Index.cshtml` (bảng nhật ký + lọc theo HĐ), badge cờ in chuyển đổi + form "In chuyển đổi"/"Bỏ cờ in chuyển đổi" + bảng nhật ký ở `Views/Invoice/Detail.cshtml`, helper `Ui.ConvPrint`/`Ui.ConvAction`, nav link "Nhật ký in chuyển đổi", endpoint minimal-API `/api/invoices/{id}/conversion-print` (POST), `/api/invoices/{id}/conversion-print/reset` (POST) và `/api/conversion-print-logs` (GET), seed 1 nhật ký in chuyển đổi demo, 6 test mới. Build Release 0 error, 44/44 test pass.- 2026-06-12 | feat: Xóa hóa đơn đã phát hành (ISSUED → DELETED) — đưa HĐ đã được CQT chấp nhận về trạng thái DELETED, ghi lại thời điểm xóa, người xóa và lý do. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Deleted`/`Invoice_Invoice_DeletedX`/`Invoice_Invoice_DeletedX_New20190705` — kiểm tra HĐ tồn tại và `InvoiceStatus == ISSUED` (`TConst.InvoiceStatus.Issued`), update `InvoiceStatus='DELETED'`, `DeleteDTimeUTC`, `DeleteBy`, `Remark`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `DeleteDTimeUTC`/`DeleteBy`/`Remark`; hằng số `TConst.InvoiceStatus.Deleted` trong `Const.Main.BE.cs`). Thêm giá trị enum `InvoiceStatus.Deleted`, trường `DeleteDTimeUTC`/`DeleteBy`/`Remark` trên `Invoice`, service `DeleteInvoiceAsync` (chặn HĐ không phải Accepted, chặn HĐ không tồn tại; đưa Accepted → Deleted, ghi `TranMessage` nhật ký), action `InvoiceController.Delete` + form "Xóa hóa đơn đã phát hành" ở `Views/Invoice/Detail.cshtml` (chỉ hiện khi HĐ Accepted) + khối thông tin xóa khi HĐ Deleted, helper `Ui.Inv` bổ sung nhãn "Đã xóa", endpoint minimal-API `/api/invoices/{id}/delete` (POST), seed 1 HĐ đã xóa demo, 3 test mới. Build Release 0 error, 47/47 test pass.
