# DEEPEN-LOG — MiniTVAN

- 2026-06-12 | feat: Xử lý hóa đơn sai sót — Điều chỉnh (tăng/giảm) & Thay thế hóa đơn đã phát hành. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Save_Adj`, `Invoice_Invoice_Save_Replace`, `Invoice_Invoice_SaveX`; hằng số `TConst.SourceInvoiceCode` INVOICEROOT/INVOICEREPLACE/INVOICEADJ và `TConst.InvoiceAdjType` ADJINCREASE/ADJDESCREASE/NORMAL). Thêm enum `SourceInvoiceCode`/`InvoiceAdjType`, trường `RefInvoiceId`/`RefTctCode`/`AdjReason` trên `Invoice`, service `AdjustAsync`/`ReplaceAsync` (HĐ gốc phải Accepted; HĐ đã điều chỉnh không điều chỉnh tiếp; thay thế hủy HĐ gốc), endpoint minimal-API `/api/invoices/{id}/adjust|replace`, action + form Razor ở `Views/Invoice/Detail.cshtml`, seed 1 HĐ điều chỉnh giảm, 5 test mới. Build Release 0 error, 12/12 test pass.
- 2026-06-12 | feat: Quản lý hạn mức hóa đơn (Invoice_license) — cấp/tăng hạn mức phát hành theo NNT + lịch sử cấp/điều chỉnh. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_license_IncreaseQty`/`Invoice_license_IncreaseQtyX` — nếu MST chưa có trong `Invoice_license` thì insert TotalQty=0 rồi cộng Qty; `Invoice_license_Get`; `Invoice_licenseCreHist_Get`; bảng `Invoice_license`/`Invoice_licenseCreHist`). Trước đó entity `InvoiceLicense`/`LicenseHist` + enum `LicenseHistType` đã có sẵn trong Models/DbContext nhưng CHƯA dùng (không service/controller/view/endpoint/seed). Thêm service `LicensesAsync`/`GetLicenseAsync`/`IncreaseLicenseAsync` (tạo mới nếu chưa có, cộng dồn TotalQty, ghi `LicenseHist` với Type Create/Increase, chặn Qty<=0 và NNT không tồn tại)/`LicenseHistsAsync`, `LicenseController` + `Views/License/Index.cshtml` (bảng hạn mức + progress còn lại + lịch sử + form cấp), helper `Ui.LicHist`, nav link, endpoint minimal-API `/api/licenses` (GET) và `/api/licenses/increase` (POST), seed 1 hạn mức 1000 cho NNT demo, 4 test mới. Build Release 0 error, 16/16 test pass.
- 2026-06-12 | feat: Bảng tổng hợp dữ liệu hóa đơn điện tử gửi CQT (Mst_GuiTongHop) — NNT lập bảng tổng hợp theo kỳ (ngày/tháng/quý/năm) gom các HĐ đã được CQT chấp nhận rồi gửi tới cơ quan thuế. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/InvoiceHub.cs`: `Mst_GuiTongHop_Create`/`Mst_GuiTongHop_CreateX`/`Mst_GuiTongHop_Get`/`Mst_GuiTongHop_GetX`; model `idN.TVAN.Common/Models/TDiepTCT/Mst_GuiTongHop.cs` + `Mst_GuiTongHopDtl.cs` + `Mst_GuiTongHopInfo.cs`; hằng số `TblMst_GuiTongHop`/`TblMst_GuiTongHopInfo` trong `Const.Main.cs`; ràng buộc `Mst_GuiTongHop_Create_Invalid_BSLThu_And_LDau` — BSLThu!=0 thì LDau=0, ngược lại LDau=1). Thêm enum `PeriodType` (LKDLieu) + `GthStatus`, entity `GuiTongHop`/`GuiTongHopDtl`, service `GuiTongHopsAsync`/`GetGuiTongHopAsync`/`CreateGuiTongHopAsync` (gom HĐ Accepted theo khoảng kỳ, chặn kỳ rỗng)/`SendGuiTongHopAsync` (round-trip 202/204), `GuiTongHopController` + `Views/GuiTongHop/Index.cshtml` + `Detail.cshtml`, helper `Ui.Period`/`Ui.Gth`, nav link, endpoint minimal-API `/api/guitonghop` (GET/POST) và `/api/guitonghop/{id}/send`, seed 1 bảng tổng hợp demo, 4 test mới. Build Release 0 error, 20/20 test pass.
- 2026-06-12 | feat: Tra cứu thông tin người nộp thuế theo MST từ cơ quan thuế (TCT_TraTTinMaSoThue) — nhập MST, hệ thống gửi yêu cầu tới CQT và trả về tên NNT, địa chỉ, cơ quan thuế quản lý; mọi lần tra cứu ghi nhật ký đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Web/Controllers/LoadDataController.cs`: `LoadTCT_TraTTinMaSoThue` — dựng XML `TRUYVAN.MST`, ký số, gọi `TCT_TraTTinMaSoThueService.WA_T2B_TCT_TraTTinMaSoThue_BaseHTTPReq`, lấy `Lst_Mst_NNT[0]` rồi tra `Mst_GovTaxID` để lấy `GovTaxName`; model `idN.TVAN.Common/Models/TCT_TraTTinMaSoThue.cs` + `RQ_/RT_TCT_TraTTinMaSoThue.cs` + `Mst_GovTaxID.cs`; bảng `Mst_GovTaxID` trong `Const.Main.cs`). Thêm enum `LookupResult` (Success/NotFound/Error), entity `TaxOffice` (danh mục CQT theo GovTaxID) + `NntLookupLog` (nhật ký tra cứu), service `TaxOfficesAsync`/`NntLookupLogsAsync`/`LookupNntByMstAsync` (MST 10/13/14 số → tìm thấy, ngược lại báo không tìm thấy; luôn ghi log), `TaxLookupController` + `Views/TaxLookup/Index.cshtml` (form tra cứu + kết quả + lịch sử + danh mục CQT), helper `Ui.Lookup`, nav link "Tra cứu MST", endpoint minimal-API `/api/tax/lookup/{mst}` (GET) và `/api/tax/offices` (GET), seed 2 cơ quan thuế + 1 nhật ký tra cứu demo, 4 test mới. Build Release 0 error, 23/23 test pass.
- 2026-06-12 | feat: Gửi/gửi lại email hóa đơn đã phát hành cho người mua (Invoice_Invoice_Support_SendMail) — chỉ gửi được cho HĐ đã được CQT chấp nhận (Accepted), người nhận lấy từ tham số hoặc EmailSend đã lưu, mọi lần gửi ghi nhật ký đối soát và cập nhật thời điểm/người gửi trên HĐ. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_SendMail`/`Invoice_Invoice_Support_SendMailX` — kiểm tra `InvoiceStatus == Issued` mới gửi, lấy `EmailSend` (nhiều địa chỉ cách nhau ';') fallback `CustomerNNTEmail`, gọi `Biz_SendMail` rồi update `SendEmailDTimeUTC`/`SendEmailBy`; `idN.TVAN.Biz/InvoiceHub.cs`: `Biz_SendMail`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `EmailSend`/`SendEmailDTimeUTC`/`SendEmailBy`/`CustomerNNTEmail`). Thêm enum `EmailSendResult` (Success/Failed), trường `EmailSend`/`SendEmailDTimeUTC`/`SendEmailBy` trên `Invoice`, entity `InvoiceEmailLog` (nhật ký gửi email), service `SendInvoiceEmailAsync` (chặn HĐ chưa Accepted, chặn email rỗng/không hợp lệ, tách nhiều địa chỉ theo ';')/`EmailLogsAsync`, `EmailLogController` + `Views/EmailLog/Index.cshtml` (bảng nhật ký + lọc theo HĐ), form "Gửi email hóa đơn" + bảng nhật ký email ở `Views/Invoice/Detail.cshtml`, helper `Ui.Email`, nav link "Nhật ký gửi email", endpoint minimal-API `/api/invoices/{id}/send-email` (POST) và `/api/email-logs` (GET), seed 1 nhật ký gửi email demo, 4 test mới. Build Release 0 error, 27/27 test pass.
- 2026-06-12 | feat: Chuyển hóa đơn về trạng thái chờ (PENDING) — giữ nguyên số hóa đơn. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_InvoiceToPending` — update `InvoiceStatus='PENDING'`, xóa `ApprDTimeUTC/ApprBy/IssuedDTimeUTC/IssuedBy/InvoiceFilePath/InvoicePDFFilePath/MailSentDTimeUTC/SendEmailDTimeUTC/SignedDate/SignBy`; hằng số `TConst.InvoiceStatus.Pending` trong `Const.Main.BE.cs`). Thêm service `ResetToPendingAsync` (chặn HĐ đang Draft hoặc đang Sent; đưa HĐ Accepted/Rejected/Cancelled về Draft, xóa `TctCode`/`RejectReason`/`SentAt`/`EmailSend`/`SendEmailDTimeUTC`/`SendEmailBy`, giữ nguyên `No`, ghi `TranMessage` nhật ký), endpoint minimal-API `/api/invoices/{id}/to-pending` (POST), action `InvoiceController.ToPending` + form "Chuyển về trạng thái chờ" ở `Views/Invoice/Detail.cshtml`, 4 test mới. Build Release 0 error, 31/31 test pass.
- 2026-06-12 | feat: Khôi phục hóa đơn đã hủy (DELETED → ISSUED) để làm thông báo sai sót — chuyển HĐ đã hủy về lại trạng thái đã phát hành, giữ nguyên mã tra cứu CQT. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_BackInvoiceStatus`/`Invoice_Invoice_Support_BackInvoiceStatusX` — chỉ nhận HĐ ở trạng thái `ISSUED`/`DELETED`, giữ `InvoiceVerifyCQTCode` nếu đầu vào không truyền mã mới, update `InvoiceStatus`; hằng số `TConst.InvoiceStatus.Issued`/`Deleted` trong `Const.Main.BE.cs`). Thêm service `RestoreInvoiceAsync` (chặn HĐ không phải Cancelled, chặn HĐ thiếu mã tra cứu; đưa Cancelled → Accepted, xóa RejectReason, ghi `TranMessage` nhật ký), endpoint minimal-API `/api/invoices/{id}/restore` (POST), action `InvoiceController.Restore` + form "Khôi phục về đã phát hành" ở `Views/Invoice/Detail.cshtml` (chỉ hiện khi HĐ đã hủy và còn mã tra cứu), 3 test mới. Build Release 0 error, 34/34 test pass.
- 2026-06-12 | feat: Xóa hóa đơn điều chỉnh/thay thế CHƯA phát hành (Invoice_Invoice_Support_DeleteInvoiceRefNo) — hủy bỏ một HĐ điều chỉnh/thay thế lập sai trước khi truyền tới CQT. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_DeleteInvoiceRefNo`/`Invoice_Invoice_Support_DeleteInvoiceRefNoX` — kiểm tra HĐ tồn tại, `InvoiceStatus == Pending`, `RefNo` không rỗng, `InvoiceNo` rỗng, `SourceInvoiceCode` phải là `INVOICEADJ`/`INVOICEREPLACE`; sau đó DELETE `Invoice_Invoice` + `Invoice_InvoiceDtl`; hằng số `TConst.SourceInvoiceCode.InvoiceADJ`/`InvoiceReplace` và `TConst.InvoiceStatus.Pending` trong `Const.Main.BE.cs`). Thêm service `DeleteAdjustReplaceAsync` (chặn HĐ không phải Draft, chặn HĐ không phải điều chỉnh/thay thế, chặn HĐ thiếu tham chiếu gốc `RefTctCode`, chặn HĐ đã có mã tra cứu `TctCode`; xóa HĐ + ghi `TranMessage` nhật ký), endpoint minimal-API `/api/invoices/{id}/delete-adjust-replace` (POST), action `InvoiceController.DeleteAdjustReplace` + form "Xóa HĐ điều chỉnh/thay thế chưa phát hành" ở `Views/Invoice/Detail.cshtml` (chỉ hiện khi HĐ Draft/Rejected và là HĐ điều chỉnh/thay thế có tham chiếu gốc), 4 test mới. Build Release 0 error, 38/38 test pass.
- 2026-06-12 | feat: In chuyển đổi hóa đơn (cờ FlagChange) — đánh dấu HĐ đã in ở dạng chuyển đổi và bỏ cờ để in lại HĐ thường. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_BackFlagChange`/`Invoice_Invoice_Support_BackFlagChangeX` — update `Invoice_Invoice.FlagChange='1'` (chưa in chuyển đổi) cho danh sách InvoiceCode; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `FlagChange` — "trạng thái hóa đơn in chuyển đổi (1 là chưa in, 0 là đã in)"; hằng số `TConst.Flag.Active`/`Inactive` trong `Const.Main.BE.cs`; luồng view in chuyển đổi ở `idN.TVAN.Web/Controllers/Invoice_InvoiceController.cs` — `inchuyendoi="1"` → `FlagChange="0"`). Thêm enum `ConversionPrintFlag` (NotPrinted=1/Printed=0) + `ConversionPrintAction` (Print/Reset), trường `FlagChange` trên `Invoice`, entity `ConversionPrintLog` (nhật ký in/bỏ cờ), service `MarkConversionPrintedAsync` (chặn HĐ chưa Accepted, chặn in lại khi đã in)/`ResetConversionPrintAsync` (chặn khi chưa in)/`ConversionPrintLogsAsync`, `ConversionPrintLogController` + `Views/ConversionPrintLog/Index.cshtml` (bảng nhật ký + lọc theo HĐ), badge cờ in chuyển đổi + form "In chuyển đổi"/"Bỏ cờ in chuyển đổi" + bảng nhật ký ở `Views/Invoice/Detail.cshtml`, helper `Ui.ConvPrint`/`Ui.ConvAction`, nav link "Nhật ký in chuyển đổi", endpoint minimal-API `/api/invoices/{id}/conversion-print` (POST), `/api/invoices/{id}/conversion-print/reset` (POST) và `/api/conversion-print-logs` (GET), seed 1 nhật ký in chuyển đổi demo, 6 test mới. Build Release 0 error, 44/44 test pass.- 2026-06-12 | feat: Xóa hóa đơn đã phát hành (ISSUED → DELETED) — đưa HĐ đã được CQT chấp nhận về trạng thái DELETED, ghi lại thời điểm xóa, người xóa và lý do. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Deleted`/`Invoice_Invoice_DeletedX`/`Invoice_Invoice_DeletedX_New20190705` — kiểm tra HĐ tồn tại và `InvoiceStatus == ISSUED` (`TConst.InvoiceStatus.Issued`), update `InvoiceStatus='DELETED'`, `DeleteDTimeUTC`, `DeleteBy`, `Remark`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `DeleteDTimeUTC`/`DeleteBy`/`Remark`; hằng số `TConst.InvoiceStatus.Deleted` trong `Const.Main.BE.cs`). Thêm giá trị enum `InvoiceStatus.Deleted`, trường `DeleteDTimeUTC`/`DeleteBy`/`Remark` trên `Invoice`, service `DeleteInvoiceAsync` (chặn HĐ không phải Accepted, chặn HĐ không tồn tại; đưa Accepted → Deleted, ghi `TranMessage` nhật ký), action `InvoiceController.Delete` + form "Xóa hóa đơn đã phát hành" ở `Views/Invoice/Detail.cshtml` (chỉ hiện khi HĐ Accepted) + khối thông tin xóa khi HĐ Deleted, helper `Ui.Inv` bổ sung nhãn "Đã xóa", endpoint minimal-API `/api/invoices/{id}/delete` (POST), seed 1 HĐ đã xóa demo, 3 test mới. Build Release 0 error, 47/47 test pass.

- 2026-06-12 | feat: Cấu hình bỏ kiểm tra ký quá 60 ngày (Invoice_Invoice_Support_Sign60Day) — bật/bỏ chế độ kiểm tra ngày ký hóa đơn khi truyền tới CQT; khi bật, HĐ có ngày ký cách thời điểm truyền > 60 ngày bị TCT từ chối. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Support_Sign60Day`/`WAS_Invoice_Invoice_Support_Sign60Day` — `bFlagChange = StringEqualIgnoreCase(objFlagChange, TConst.Flag.Inactive)` (FlagChange='0' → bỏ check), `bIsCheck60day = !bFlagChange`; cờ `bIsCheck60day` khai báo ở `idN.TVAN.Biz/Common.cs` (mặc định `true`); logic chặn ở `Invoice_Invoice_Support_InvoiceToPending` (dòng 54263) và `zTemp.cs` (dòng 2448): `TimeSpan checkDate = dtimeSys - SignedDate; if (TotalDate > 60 && bIsCheck60day) throw ...InvalidSignedDate`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `SignedDate` — "Ngày ký Hoá đơn cho Client truyền"; hằng số `TConst.Flag.Active`/`Inactive` trong `Const.Main.BE.cs`). Thêm enum `Sign60DayFlag` (Check=1/Uncheck=0), trường `SignedDate` trên `Invoice`, entity `SystemSetting` (cấu hình theo tổ chức), service `GetSettingAsync` (tự tạo mặc định Check)/`SetSign60DayAsync` + chặn ký >60 ngày trong `TransmitAsync` (chỉ khi Check và có SignedDate), `SettingController` + `Views/Setting/Index.cshtml` (trạng thái + form bật/bỏ), helper `Ui.Sign60Day`, nav link "Cấu hình", endpoint minimal-API `/api/settings/sign60day` (GET/POST), seed cấu hình mặc định + ngày ký cho HĐ demo, 5 test mới. Build Release 0 error, 52/52 test pass.
- 2026-06-12 | feat: Ký lại hóa đơn đã phát hành (Invoice_Invoice_ReSign) — cập nhật nội dung hóa đơn đã ký (base64 XML) + đường dẫn file XML, đánh dấu cờ FlagHotfix='1', ghi thời điểm/người duyệt (ApprDTimeUTC/ApprBy) và ghi nhật ký đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_ReSign`/`WAS_Invoice_Invoice_ReSign`/`Invoice_Invoice_ReSignX` — kiểm tra HĐ tồn tại, `InvoiceNo` không rỗng, ghi file XML đã ký, set `InvoiceFileSpec`/`InvoiceFilePath`/`FlagHotfix='1'`/`ApprDTimeUTC`/`ApprBy`/`InvoiceStatus='APPROVED'`; điều kiện lọc `FlagHotfix is null and InvoiceStatus='ISSUED'`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `InvoiceFileSpec`/`InvoiceFilePath`/`ApprDTimeUTC`/`ApprBy`). Thêm enum `HotfixFlag` (None/Hotfixed), trường `InvoiceFileSpec`/`InvoiceFilePath`/`FlagHotfix`/`ApprDTimeUTC`/`ApprBy` trên `Invoice`, entity `ReSignLog` (nhật ký ký lại), service `ReSignAsync` (chặn HĐ chưa Accepted, chặn HĐ đã ký lại, chặn thiếu nội dung đã ký)/`ReSignLogsAsync`, `ReSignLogController` + `Views/ReSignLog/Index.cshtml` (bảng nhật ký + lọc theo HĐ), badge cờ ký lại + form "Ký lại hóa đơn" + bảng nhật ký ở `Views/Invoice/Detail.cshtml`, helper `Ui.Hotfix`, nav link "Nhật ký ký lại HĐ", endpoint minimal-API `/api/invoices/{id}/re-sign` (POST) và `/api/re-sign-logs` (GET), seed 1 nhật ký ký lại demo, 5 test mới. Build Release 0 error, 57/57 test pass.
- 2026-06-12 | feat: Duyệt hóa đơn (Invoice_Invoice_Approved) — bước duyệt HĐ trước khi phát hành: PENDING → APPROVED, ghi đường dẫn file XML/PDF hóa đơn đã duyệt, thời điểm & người duyệt (ApprDTimeUTC/ApprBy); hỗ trợ bỏ duyệt (APPROVED → PENDING) xóa dấu vết duyệt; mọi lần duyệt/bỏ duyệt ghi nhật ký đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Approved`/`Invoice_Invoice_ApprovedX` — `Invoice_Invoice_CheckDB` chỉ nhận HĐ ở `TConst.InvoiceStatus.Pending`, yêu cầu `InvoiceNo` không rỗng, update `InvoiceStatus='APPROVED'`, `InvoiceFilePath`, `InvoicePDFFilePath`, `ApprDTimeUTC`, `ApprBy`, `Remark`; hằng số `TConst.InvoiceStatus.Pending`/`Approved` trong `Const.Main.BE.cs`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `InvoiceFilePath`/`InvoicePDFFilePath`/`ApprDTimeUTC`/`ApprBy`). Thêm enum `InvoiceStatus.Approved` + `ApproveAction` (Approve/Unapprove), trường `InvoicePDFFilePath` trên `Invoice` (dùng chung `InvoiceFilePath`/`ApprDTimeUTC`/`ApprBy`), entity `ApproveLog` (nhật ký duyệt), service `ApproveAsync` (chặn HĐ không ở trạng thái chờ, chặn HĐ thiếu số)/`UnapproveAsync` (chặn HĐ chưa duyệt)/`ApproveLogsAsync`, `ApproveLogController` + `Views/ApproveLog/Index.cshtml` (bảng nhật ký + lọc theo HĐ), badge trạng thái "Đã duyệt" + form "Duyệt hóa đơn"/"Bỏ duyệt" + bảng nhật ký ở `Views/Invoice/Detail.cshtml`, helper `Ui.ApproveAction`, nav link "Nhật ký duyệt HĐ", endpoint minimal-API `/api/invoices/{id}/approve` (POST), `/api/invoices/{id}/unapprove` (POST) và `/api/approve-logs` (GET), seed 1 nhật ký duyệt demo, 5 test mới. Build Release 0 error, 62/62 test pass.
- 2026-06-12 | feat: Phát hành / Ngừng hoạt động mẫu hóa đơn (vòng đời Invoice_TempInvoice) — mẫu hóa đơn chuyển từ chờ (PENDING) sang đang sử dụng (ISSUED) khi phát hành, và sang ngừng hoạt động (INACTIVE) khi hết hiệu lực; mẫu ngừng hoạt động không còn được cấp số. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_TempInvoice_Issued`/`Invoice_TempInvoice_IssuedX` — `Invoice_TempInvoice_CheckDB` chỉ nhận mẫu `TInvoiceStatus=PENDING` + `FlagActive=Active`, chặn khi `StartInvoiceNo`/`EndInvoiceNo` = 0 (`InvalidEndInvoiceNoAndStatrInvoiceNo`) và khi `EffDateStart` trước ngày hệ thống (`InvalidIEffDateStartAfterSysDate`), update `TInvoiceStatus=ISSUED`/`EffDateStart`/`IssuedDTimeUTC`/`IssuedBy`; `Invoice_TempInvoice_InActive`/`Invoice_TempInvoice_InActiveX` — chỉ nhận mẫu `TInvoiceStatus=ISSUED` + `FlagActive=Active`, update `TInvoiceStatus=INACTIVE`/`EffDateEnd`/`InActiveDTimeUTC`/`InActiveBy`/`FlagActive=Inactive`; hằng số `TConst.TInvoiceStatus.Pending`/`Issued` trong `Const.Main.BE.cs`). Trước đó entity `InvoiceTemplate` + enum `TemplateStatus` (Draft/Issued/Inactive) đã có sẵn nhưng CHƯA có thao tác vòng đời (chỉ đọc + cấp số). Thêm service `IssueTemplateAsync` (chặn mẫu không ở trạng thái chờ, chặn mẫu đã ngừng hoạt động, chặn dải số không hợp lệ, chặn ngày bắt đầu trước hiện tại; đưa mẫu sang Issued, ghi EffDateStart, ghi `TranMessage` nhật ký) và `InactivateTemplateAsync` (chặn mẫu không phải đang sử dụng, chặn mẫu đã ngừng; đưa mẫu sang Inactive, ghi EffDateEnd = hôm nay, tắt FlagActive, ghi nhật ký), action `InvoiceTemplateController.Issue`/`Inactivate` + 2 modal form "Phát hành"/"Ngừng" ở `Views/InvoiceTemplate/Index.cshtml`, helper `Ui.TemplateAction`, endpoint minimal-API `/api/templates/{id}/issue` (POST) và `/api/templates/{id}/inactivate` (POST), seed 1 mẫu 1C26TAB ở trạng thái Draft demo, 6 test mới. Sửa 5 test `AllocateNo_*` có sẵn (dùng HĐ nháp chưa có số qua helper `SetupNoNo` thay vì `Setup` tự cấp số). Build Release 0 error, 74/74 test pass.
- 2026-06-12 | feat: Nhận kết quả phản hồi từ CQT cho hóa đơn đã gửi (Invoice_Invoice_TCTReceive) — CQT trả kết quả phát hành cho HĐ đang chờ phản hồi: mã loại thông điệp 202 = phát hành thành công (có mã xác thực CQT) → HĐ chuyển ACCEPTED + ghi mã tra cứu; 204 = phát hành thất bại → HĐ chuyển REJECTED + ghi mã lỗi/lý do; mọi lần nhận ghi nhật ký đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_TCTReceive`/`Invoice_Invoice_TCTReceiveX` — `Invoice_Invoice_CheckDB` yêu cầu HĐ tồn tại, switch theo `MLTDiep`: `202` → `Invoice_Invoice_Process202` (update `TCTChapNhan`/`TCTMaLoi`/`TCTLyDo`/`InvoiceVerifyCQTCode`=`MaCQT`, rồi `Invoice_Invoice_IssuedX_New20200321` phát hành + gửi mail), `204`+root `200` → `Invoice_Invoice_Process204_200` (update `InvoiceStatus`/`TCTChapNhan`/`TCTMaLoi`/`TCTLyDo`); hằng số `TConst.TCTStatus.Accept`/`Reject` trong `Const.Main.BE.cs`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `MLTDiep`/`MLTDiepRoot`/`TCTChapNhan`/`TCTMaLoi`/`TCTLyDo`/`InvoiceVerifyCQTCode`). Thêm enum `TctMessageType` (Success202/Fail204) + `TctAcceptStatus` (Accept/Reject), trường `MltDiep`/`TctChapNhan`/`TctMaLoi`/`TctLyDo`/`TctReceiveDTimeUTC` trên `Invoice`, entity `TctReceiveLog` (nhật ký nhận kết quả CQT), service `ReceiveTctResultAsync` (chặn HĐ không ở trạng thái Sent, chặn 202 thiếu mã xác thực; 202 → Accepted + ghi TctCode, 204 → Rejected + ghi RejectReason; ghi `TranMessage` + `TctReceiveLog`)/`TctReceiveLogsAsync`, `TctReceiveLogController` + `Views/TctReceiveLog/Index.cshtml` (bảng nhật ký + lọc theo HĐ), form "Nhận kết quả từ CQT" + bảng nhật ký ở `Views/Invoice/Detail.cshtml` (chỉ hiện khi HĐ đang Sent), helper `Ui.TctMsg`/`Ui.TctAccept`, nav link "Nhật ký nhận KQ CQT", endpoint minimal-API `/api/invoices/{id}/tct-receive` (POST) và `/api/tct-receive-logs` (GET), seed 1 nhật ký nhận kết quả 202 demo, 5 test mới. Build Release 0 error, 79/79 test pass.
- 2026-06-12 | feat: Cập nhật nội dung hóa đơn sau khi đã cấp số (Invoice_Invoice_UpdAfterAllocated) — cho phép sửa người mua, phương thức thanh toán, tiền hàng/thuế suất và ngày hóa đơn khi HĐ đang ở trạng thái chờ (PENDING) và ĐÃ có số; ràng buộc ngày hóa đơn không được là ngày tương lai và phải nằm giữa ngày HĐ liền trước/liền sau trong cùng mẫu; mọi lần cập nhật ghi nhật ký đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_UpdAfterAllocated`/`Invoice_Invoice_UpdAfterAllocatedX` — `Invoice_Invoice_CheckDB` chỉ nhận HĐ `TConst.InvoiceStatus.Pending`, chặn khi `SourceInvoiceCode != INVOICEROOT` (`..._SourceInvoiceCode`), chặn khi `InvoiceNo` rỗng (`..._InvalidInvoiceNo`), chặn ngày HĐ là ngày tương lai (`..._InvaliInvoiceDateUTCAfterSysDate`), và 2 check SQL theo `TInvoiceCode`: ngày HĐ không được trước ngày HĐ liền trước (`..._Invalid_Before_InvoiceDateUTC`) / không được sau ngày HĐ liền sau (`..._Invalid_Last_InvoiceDateUTC`); cập nhật `PaymentMethodCode`/`CustomerNNT*`/`CustomerMST`/`InvoiceDateUTC`/`TotalValInvoice`/`TotalValVAT`/`TotalValPmt`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `PaymentMethodCode`; danh mục `Mst_PaymentMethods`). Thêm enum `PaymentMethod` (Cash/Transfer/CashOrTransfer), trường `PaymentMethod` trên `Invoice`, entity `InvoiceUpdateLog` (nhật ký cập nhật sau cấp số), service `UpdateAfterAllocatedAsync` (chặn HĐ không ở trạng thái chờ, chặn HĐ không phải gốc, chặn HĐ chưa có số, chặn ngày tương lai, chặn ngày lệch thứ tự so với HĐ liền trước/liền sau cùng mẫu; cập nhật nội dung + ghi `InvoiceUpdateLog` + `TranMessage`)/`UpdateLogsAsync`, action `InvoiceController.UpdateAfterAllocated` + form "Cập nhật nội dung sau cấp số" + bảng nhật ký ở `Views/Invoice/Detail.cshtml`, helper `Ui.Payment`, endpoint minimal-API `/api/invoices/{id}/update-after-allocated` (POST) và `/api/invoice-update-logs` (GET), seed 1 nhật ký cập nhật demo cho HĐ nháp đã có số, 5 test mới. Build Release 0 error, 84/84 test pass.
- 2026-06-12 | feat: Tăng số hóa đơn cuối (EndInvoiceNo) của mẫu hóa đơn — mở rộng dải số được cấp phát cho mẫu đang sử dụng. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_TempInvoice_IncreaseEndInvoiceNo`/`Invoice_TempInvoice_IncreaseEndInvoiceNoX` và `Invoice_TempInvoice_IncreaseQtyInvoiceNo`/`Invoice_TempInvoice_IncreaseQtyInvoiceNoX` — `Invoice_TempInvoice_CheckDB` chỉ nhận mẫu `TInvoiceStatus=ISSUED` + `FlagActive=Active`; chặn khi `objEndInvoiceNo_Input < objEndInvoiceNo_Current` (`..._InvalidValue`); 2 check SQL: `(t.EndInvoiceNo - t.StartInvoiceNo) < t.QtyUsed` và `IsNull(Convert(int, t.LastInvoiceNo), 0) > t.EndInvoiceNo`; update `EndInvoiceNo`/`LUDTimeUTC`/`LUBy`/`Remark`; hằng số `TConst.TInvoiceStatus.Issued`/`TConst.Flag.Active` trong `Const.Main.BE.cs`). Trước đó entity `InvoiceTemplate` đã có thao tác vòng đời (phát hành/ngừng) nhưng CHƯA có thao tác mở rộng dải số. Thêm enum `TemplateRangeAction` (IncreaseEndNo), entity `TemplateRangeLog` (nhật ký mở rộng dải số: OldEndInvoiceNo/NewEndInvoiceNo/Remark/By), service `IncreaseTemplateEndNoAsync` (chặn mẫu không phải đang sử dụng, chặn mẫu đã ngừng, chặn số cuối mới <= số cuối hiện tại, chặn dải mới nhỏ hơn số đã dùng, chặn số cuối đã cấp vượt số cuối mới; cập nhật EndInvoiceNo + ghi `TemplateRangeLog` + `TranMessage`)/`TemplateRangeLogsAsync`, action `InvoiceTemplateController.IncreaseEndNo` + modal "Tăng dải số" + bảng nhật ký mở rộng dải số ở `Views/InvoiceTemplate/Index.cshtml`, helper `Ui.RangeAction`, endpoint minimal-API `/api/templates/{id}/increase-end-no` (POST) và `/api/template-range-logs` (GET), seed 1 nhật ký tăng dải số demo (1000 → 2000), 6 test mới. Build Release 0 error, 90/90 test pass.
- 2026-06-12 | feat: Phát hành hóa đơn (Invoice_Invoice_Issued) — bước phát hành HĐ sau khi duyệt: APPROVED → ISSUED, ghi thời điểm & người phát hành (IssuedDTimeUTC/IssuedBy), cập nhật email người nhận (EmailSend) + thời điểm/người gửi email (SendEmailDTimeUTC/SendEmailBy); mọi lần phát hành ghi nhật ký đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `WAS_Invoice_Invoice_Issued`/`Invoice_Invoice_Issued` — `Invoice_Invoice_CheckDB` chỉ nhận HĐ ở `TConst.InvoiceStatus.Approved`; `idN.TVAN.Biz/zTemp.cs`: `Invoice_Invoice_IssuedX_New20190905` — update `InvoiceStatus='ISSUED'`, `IssuedDTimeUTC`/`IssuedBy`, `SendEmailDTimeUTC`/`SendEmailBy`, `EmailSend`, `Remark`; hằng số `TConst.InvoiceStatus.Approved`/`Issued` trong `Const.Main.BE.cs`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `IssuedDTimeUTC`/`IssuedBy`). Thêm enum `IssueAction` (Issue), trường `IssuedDTimeUTC`/`IssuedBy` trên `Invoice`, entity `IssueLog` (nhật ký phát hành), service `IssueAsync` (chặn HĐ không ở trạng thái đã duyệt, chặn HĐ thiếu số, chặn email không hợp lệ; đưa HĐ sang Accepted, ghi thời điểm/người phát hành + email, ghi `IssueLog` + `TranMessage`)/`IssueLogsAsync`, action `InvoiceController.Issue` + form "Phát hành hóa đơn" ở `Views/Invoice/Detail.cshtml` (chỉ hiện khi HĐ đã duyệt) + bảng nhật ký phát hành, `IssueLogController` + `Views/IssueLog/Index.cshtml`, helper `Ui.IssueAction`, nav link "Nhật ký phát hành HĐ", endpoint minimal-API `/api/invoices/{id}/issue` (POST) và `/api/issue-logs` (GET), seed 1 nhật ký phát hành demo cho HĐ đã phát hành, 4 test mới. Build Release 0 error, 94/94 test pass.
- 2026-06-12 | feat: Hủy hóa đơn đang chờ/đã duyệt (Invoice_Invoice_Cancel) — hủy HĐ ở trạng thái chờ (PENDING) hoặc đã duyệt (APPROVED) → CANCELED, ghi thời điểm hủy (CancelDTimeUTC), người hủy (CancelBy) và lý do (Remark); mọi lần hủy ghi nhật ký đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_Cancel`/`WAS_Invoice_Invoice_Cancel` — `Invoice_Invoice_CheckDB` chỉ nhận HĐ ở `"PENDING, APPROVED"`, chặn khi HĐ PENDING mà `InvoiceNo` rỗng (`Invoice_Invoice_Cancel_InvoiceNoIsNotNull`); `idN.TVAN.Biz/zTemp.cs`: `Invoice_Invoice_CancelX_New20190705` — update `InvoiceStatus='CANCELED'`, `CancelDTimeUTC`, `CancelBy`, `Remark`, `LogLUDTimeUTC`/`LogLUBy`; hằng số `TConst.InvoiceStatus.Canceled` trong `Const.Main.BE.cs`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `CancelDTimeUTC`/`CancelBy`). Thêm enum `CancelAction` (Cancel), trường `CancelDTimeUTC`/`CancelBy` trên `Invoice`, entity `CancelInvoiceLog` (nhật ký hủy), service `CancelInvoiceAsync` (chặn HĐ không ở trạng thái chờ/đã duyệt, chặn HĐ chờ chưa có số; đưa HĐ sang Cancelled, ghi thời điểm/người hủy + lý do, ghi `CancelInvoiceLog` + `TranMessage`)/`CancelLogsAsync`, action `InvoiceController.CancelInvoice` + form "Hủy hóa đơn" ở `Views/Invoice/Detail.cshtml` (hiện khi HĐ chờ đã có số hoặc đã duyệt) + bảng nhật ký hủy, `CancelLogController` + `Views/CancelLog/Index.cshtml`, helper `Ui.CancelAction`, nav link "Nhật ký hủy HĐ", endpoint minimal-API `/api/invoices/{id}/cancel-invoice` (POST) và `/api/cancel-logs` (GET), seed 1 nhật ký hủy demo, 5 test mới. Build Release 0 error, 99/99 test pass.
- 2026-06-12 | feat: Cấp số hóa đơn khởi tạo từ MÁY TÍNH TIỀN (MTT) + sinh mã CQT trên hóa đơn MTT (MCCQTMTT) — hóa đơn loại MTT (Mẫu số có ký tự thứ 4 = 'M', theo Thông tư 32/2025/TT-BTC) khi cấp số KHÔNG sinh mã tra cứu thông thường mà sinh "Mã của CQT trên hóa đơn khởi tạo từ máy tính tiền" định dạng M<C2>-<yy>-<MCCQT>-<MMdd><seq7> (23 ký tự). Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_AllocatedInvoiceTypeM`/`Invoice_Invoice_AllocatedInvoiceTypeMX` — `Invoice_Invoice_CheckDB` chỉ nhận HĐ `TConst.InvoiceStatus.Pending`, chặn khi `FormNo.Substring(3,1) != "M"` (`..._InvalidMCCQT`), chặn khi `Mst_NNT.MCCQT` rỗng hoặc độ dài != 5 (`..._InvalidMCCQT`), chặn khi HĐ đã có `InvoiceNo` (`..._NotAllowAllocatedInv`), chặn ngày HĐ trước `LastInvoiceDateUTC`/`EffDateStart` hoặc là ngày tương lai; `Invoice_Invoice_GenMCCQTMTTTypeM`/`Invoice_Invoice_GenMCCQTMTTTypeMX` — sinh mã `M{Sign}-{yy}-{MCCQT}-{MMdd}{seq7}` với `Seq_Common_Raw("Seq_InvoiceMCCQTMTT")`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `MCCQTMTT`; `Mst_NNT.MCCQT`; `idN.TVAN.Common/ModelsUI/TDiep.cs`: `MCCQT`). Thêm enum `InvoiceTypeM` (Normal/Machine), trường `MCCQT` trên `Nnt` + `MCCQTMTT` trên `Invoice`, service `AllocateInvoiceNoTypeMAsync` (chặn HĐ không phải Draft, chặn HĐ đã có số, chặn mẫu không phải loại MTT, chặn NNT thiếu MCCQT 5 ký tự, chặn ngày tương lai/trước hiệu lực mẫu/trước ngày cấp số gần nhất; cấp số kế tiếp từ mẫu + ghi `InvoiceNoAllocLog` + `TranMessage`, KHÔNG sinh TctCode) và `GenMccqtMttAsync` (chặn HĐ không phải Draft/không phải loại MTT/thiếu MCCQT; sinh mã 23 ký tự + ghi `TranMessage`), helper `Ui.InvoiceType`, badge "HĐ máy tính tiền" + hiển thị MCCQTMTT + 2 form "Cấp số HĐ máy tính tiền"/"Sinh mã CQT máy tính tiền" ở `Views/Invoice/Detail.cshtml`, action `InvoiceController.AllocateNoTypeM`/`GenMccqtMtt`, endpoint minimal-API `/api/invoices/{id}/allocate-no-type-m` (POST) và `/api/invoices/{id}/gen-mccqt-mtt` (POST), seed NNT MCCQT="A1B2C" + mẫu 1C2MAA + 1 HĐ MTT đã cấp số & sinh mã demo, 5 test mới. Build Release 0 error, 104/104 test pass.
- 2026-06-12 | feat: Tạo biên bản đính kèm hóa đơn (luồng TaoBienBan) — đính kèm biên bản hủy/điều chỉnh/thay thế cho hóa đơn: lưu tên file, nội dung file (base64), đường dẫn file đã lưu và lý do hủy/điều chỉnh/thay thế vào hóa đơn; mọi lần tạo ghi nhật ký đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Web/Controllers/Invoice_InvoiceController.cs`: `TaoBienBan` — kiểm tra `invoiceCode` không rỗng, lấy HĐ theo `InvoiceCode`, set `DeleteReason` + `AttachedDelFileName`/`AttachedDelFileSpec` (base64) + `AttachedDelFilePath` rồi `WA_Invoice_Invoice_Upd`; các action tải mẫu `DownloadMauBienBanHuyHoaDon`/`DownloadMauBienBanDieuChinhHoaDon`/`DownloadMauBienBanThayTheHoaDon`; model `idN.TVAN.Common/Models/Invoice_Invoice.cs`: `AttachedDelFileSpec`/`AttachedDelFilePath`/`DeleteReason`/`AttachedDelFileName`). Thêm enum `RecordType` (Huy/DieuChinh/ThayThe), trường `AttachedDelFileName`/`AttachedDelFileSpec`/`AttachedDelFilePath`/`DeleteReason` trên `Invoice`, entity `InvoiceRecordLog` (nhật ký tạo biên bản), service `CreateRecordAsync` (chặn HĐ không tồn tại, chặn thiếu tên file; lưu file + lý do vào HĐ, ghi `InvoiceRecordLog` + `TranMessage`)/`RecordLogsAsync`, action `InvoiceController.CreateRecord` + form "Tạo biên bản đính kèm" + bảng nhật ký biên bản ở `Views/Invoice/Detail.cshtml`, `RecordLogController` + `Views/RecordLog/Index.cshtml`, helper `Ui.Record`, nav link "Nhật ký biên bản", endpoint minimal-API `/api/invoices/{id}/record` (POST) và `/api/record-logs` (GET), seed 1 biên bản hủy demo cho HĐ đã hủy, 3 test mới. Build Release 0 error, 107/107 test pass.
- 2026-06-12 | feat: Cấp số + Duyệt + Phát hành trong MỘT bước (Invoice_Invoice_AllocatedAndApprovedAndIssued) — gộp 3 thao tác tuần tự trên cùng một hóa đơn đang chờ (PENDING) chưa có số thành một thao tác duy nhất: (1) cấp số kế tiếp từ mẫu, (2) duyệt hóa đơn (PENDING → APPROVED), (3) phát hành hóa đơn (APPROVED → ISSUED). Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_AllocatedAndApprovedAndIssued`/`Invoice_Invoice_AllocatedAndApprovedAndIssued_New20191225` — trong một transaction gọi tuần tự `Invoice_Invoice_AllocatedInvX_New20191206` → `Invoice_Invoice_ApprovedX_New20190705` → `Invoice_Invoice_IssuedX_New20190905` → `Email_BatchSendEmail_SaveAndSendX`; tham số `objInvoiceCode`/`objInvoiceDateUTC`/`objInvoiceFilePath`/`objInvoicePDFFilePath`/`objEmailSend`/`objRemark`). Thêm service `AllocateApproveIssueAsync` (chặn HĐ không ở trạng thái chờ, chặn HĐ đã có số, chặn ngày tương lai/trước hiệu lực mẫu/trước ngày cấp số gần nhất, chặn mẫu hết dải số, chặn email không hợp lệ; cấp số từ mẫu + ghi `InvoiceNoAllocLog`, duyệt + ghi `ApproveLog`, phát hành + ghi `IssueLog`, mỗi bước ghi `TranMessage`), action `InvoiceController.AllocateApproveIssue` + form "Cấp số + Duyệt + Phát hành" ở `Views/Invoice/Detail.cshtml` (hiện khi HĐ Draft chưa có số), endpoint minimal-API `/api/invoices/{id}/allocate-approve-issue` (POST), 5 test mới. Build Release 0 error, 112/112 test pass.
- 2026-06-12 | feat: Gửi mẫu hóa đơn tới CQT (SentTCT) + CQT phát hành mẫu (TCTIssued) — mẫu hóa đơn chuyển từ chờ (PENDING) sang ĐÃ GỬI CQT (SENTTCT) khi gửi đăng ký tới cơ quan thuế (ghi mã V tham chiếu file + thông báo CQT + thời điểm/người gửi), rồi nhận kết quả phát hành từ CQT: chấp nhận (ACCEPT) → mẫu chuyển ĐANG SỬ DỤNG (ISSUED), từ chối (REJECT) → mẫu quay về chờ (PENDING); mọi lần gửi/nhận ghi nhật ký đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_TempInvoice_SentTCT`/`Invoice_TempInvoice_SentTCTX` — `Invoice_TempInvoice_CheckDB` chỉ nhận mẫu `TInvoiceStatus` ∈ {PENDING, SENTTCT, ISSUED} + `FlagActive=Active`, chặn khi `StartInvoiceNo`/`EndInvoiceNo` = 0 (`..._InvalidEndInvoiceNoAndStatrInvoiceNo`), gọi `OS_RptSv_Send_Record` lấy `MTDiep` (mã V) + `message`, update `TInvoiceStatus=SENTTCT`/`TCTRefNo`/`TCTMessage`/`SentTCTDTime`/`SentTCTBy`; `Invoice_TempInvoice_TCTIssuedX` — `bIsSuccess = TCTChapNhan == TConst.TCTStatus.Accept`, update `TCTChapNhan`/`TCTChapNhanDTime`/`TCTMessage`/`TInvoiceStatus` = ACCEPT→ISSUED / REJECT→PENDING; hằng số `TConst.TInvoiceStatus.SentTCT`/`Issued`/`Pending` và `TConst.TCTStatus.Accept`/`Reject` trong `Const.Main.BE.cs`; model `idN.TVAN.Common/Models/Invoice_TempInvoice.cs`: `TCTRefNo`/`TCTMessage`/`SentTCTDTime`/`SentTCTBy`/`TCTChapNhan`/`TCTChapNhanDTime`). Trước đó entity `InvoiceTemplate` + enum `TemplateStatus` (Draft/Issued/Inactive) đã có sẵn nhưng CHƯA có luồng gửi/nhận CQT. Thêm enum `TemplateStatus.SentTct` + `TemplateTctAction` (SendTct/ReceiveTct), trường `TCTRefNo`/`TCTMessage`/`SentTCTDTime`/`SentTCTBy`/`TCTChapNhan`/`TCTChapNhanDTime` trên `InvoiceTemplate`, entity `TemplateTctLog` (nhật ký gửi/nhận CQT), service `SendTemplateToTctAsync` (chặn mẫu không ở trạng thái chờ, chặn mẫu đã ngừng, chặn dải số không hợp lệ; đưa mẫu sang SentTct, sinh mã V, ghi nhật ký + `TranMessage`) và `ReceiveTemplateTctResultAsync` (chặn mẫu chưa gửi CQT; ACCEPT → Issued + ghi EffDateStart, REJECT → Draft; ghi `TCTChapNhan`/`TCTChapNhanDTime`/`TCTMessage` + nhật ký + `TranMessage` 202/204)/`TemplateTctLogsAsync`, action `InvoiceTemplateController.SendTct`/`ReceiveTct` + 2 modal "Gửi CQT"/"Nhận KQ CQT" + badge trạng thái "Đã gửi CQT" + mã V + bảng nhật ký gửi/nhận CQT ở `Views/InvoiceTemplate/Index.cshtml`, helper `Ui.TemplateTctAction` + cập nhật `Ui.Template` cho SentTct, endpoint minimal-API `/api/templates/{id}/send-tct` (POST), `/api/templates/{id}/receive-tct` (POST) và `/api/template-tct-logs` (GET), seed 1 mẫu 1C26TAC ở trạng thái SentTct + 1 nhật ký gửi CQT demo, 5 test mới. Build Release 0 error, 117/117 test pass. Verify: app chạy (DB mới) → GET /InvoiceTemplate trả 200 có nút "Gửi CQT"/"Nhận KQ CQT" + bảng nhật ký + badge "Đã gửi CQT"; GET /api/template-tct-logs trả 200 kèm bản ghi seed.
- 2026-06-12 | feat: Trường tùy chỉnh hóa đơn (Invoice_CustomField / Invoice_DtlCustomField) — mỗi tổ chức tự định nghĩa các trường tùy chỉnh trên hóa đơn (InvCF1..InvCF10) và trên danh sách hàng hóa (InvDCF1..InvDCF5): tạo mới/cập nhật theo mã (khóa nghiệp vụ OrgId+Code), bật/ngừng dùng (FlagActive) và xóa. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Web/Controllers/Invoice_CustomFieldController.cs`: `Index`/`Create`/`Update`/`Delete`; `idN.TVAN.Biz/Master.cs`: `Invoice_CustomField_Create_New20190703`/`Invoice_CustomField_Update_New20190703`/`Invoice_CustomField_Delete`/`Invoice_CustomField_CheckDB` (chặn thiếu OrgID/Code, chặn trùng khi tạo, chặn không tồn tại khi sửa/xóa) và các hàm tương ứng cho `Invoice_DtlCustomField`; model `idN.TVAN.Common/Models/Invoice_CustomField.cs` + `Invoice_DtlCustomField.cs`; `Scripts/mine/invoice_CustomField.js` — DBPhysicalType luôn = "TEXT"). Thêm enum `DBPhysicalType` (Text/Number/Date), entity `InvoiceCustomField` + `InvoiceDtlCustomField` (Code/Name/DBPhysicalType/FlagActive/UpdatedAt/UpdatedBy), service `InvoiceCustomFieldsAsync`/`InvoiceDtlCustomFieldsAsync`/`SaveInvoiceCustomFieldAsync`/`SaveInvoiceDtlCustomFieldAsync` (lưu lần đầu = tạo, lưu lại = cập nhật; chặn thiếu mã/tên)/`DeleteInvoiceCustomFieldAsync`/`DeleteInvoiceDtlCustomFieldAsync`, `CustomFieldController` + `Views/CustomField/Index.cshtml` (2 bảng + form lưu/xóa cho HĐ và hàng hóa), nav link "Trường tùy chỉnh HĐ", endpoint minimal-API `/api/custom-fields` (GET/POST), `/api/custom-fields/dtl` (GET/POST), `/api/custom-fields/{code}` (DELETE), `/api/custom-fields/dtl/{code}` (DELETE), seed 2 trường HĐ + 1 trường hàng hóa demo, 5 test mới. Build Release 0 error 0 warning, 121/121 test pass.
- 2026-06-12 | feat: Cập nhật thông tin liên hệ của NNT in trên mẫu hóa đơn (Invoice_TempInvoice_SupportUpdEmailAndAddress) — sửa tên đơn vị, địa chỉ, điện thoại, email, website và cờ dấu phân cách động (FlagStyleComma) hiển thị trên hóa đơn phát hành; ghi lại thời điểm & người cập nhật để đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_TempInvoice_SupportUpdEmailAndAddress` — `Invoice_TempInvoice_CheckDB` yêu cầu mẫu tồn tại, chặn khi cập nhật `NNTName` mà rỗng (`..._InvalidNNTName`), update `NNTName`/`NNTWebsite`/`NNTPhone`/`NNTEmail`/`NNTAddress`/`FlagStyleComma`/`LogLUDTimeUTC`/`LogLUBy`; model `idN.TVAN.Common/Models/Invoice_TempInvoice.cs`: `NNTName`/`NNTAddress`/`NNTPhone`/`NNTEmail`/`NNTWebsite`/`FlagStyleComma`). Thêm trường `NNTName`/`NNTAddress`/`NNTPhone`/`NNTEmail`/`NNTWebsite`/`FlagStyleComma`/`ContactUpdatedAt`/`ContactUpdatedBy` trên `InvoiceTemplate`, service `UpdateTemplateContactAsync` (chặn mẫu không tồn tại, chặn tên đơn vị rỗng; cập nhật thông tin liên hệ + ghi `TranMessage` nhật ký), action `InvoiceTemplateController.UpdateContact` + modal "Thông tin liên hệ trên mẫu hóa đơn" + hiển thị liên hệ ở `Views/InvoiceTemplate/Index.cshtml`, endpoint minimal-API `/api/templates/{id}/contact` (POST), seed thông tin liên hệ demo cho mẫu 1C26TAA, 3 test mới. Build Release 0 error 0 warning, 124/124 test pass.
- 2026-06-12 | feat: Cập nhật số tài khoản & tên ngân hàng của NNT in trên mẫu hóa đơn (Invoice_TempInvoice_SupportUpdAccNoAndBankName) — sửa số tài khoản (NNTAccNo) và tên ngân hàng (NNTBankName) hiển thị trên hóa đơn phát hành; ghi lại thời điểm & người cập nhật để đối soát. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_TempInvoice_SupportUpdAccNoAndBankName` — `Invoice_TempInvoice_CheckDB` yêu cầu mẫu tồn tại, chặn khi cập nhật `NNTAccNo` mà rỗng (`..._InvalidNNTAccNo`) và khi cập nhật `NNTBankName` mà rỗng (`..._InvalidNNTBankName`), update `NNTAccNo`/`NNTBankName`/`LogLUDTimeUTC`/`LogLUBy`; model `idN.TVAN.Common/Models/Invoice_TempInvoice.cs`: `NNTAccNo`/`NNTBankName`; controller `idN.TVAN.Web/Controllers/Invoice_TempInvoiceController.cs` — `strFt_Cols_Upd` gồm `NNTAccNo`/`NNTBankName` khi cập nhật mẫu đã phát hành). Thêm trường `NNTAccNo`/`NNTBankName` trên `InvoiceTemplate`, service `UpdateTemplateBankAsync` (chặn mẫu không tồn tại, chặn số tài khoản rỗng, chặn tên ngân hàng rỗng; cập nhật số tài khoản/ngân hàng + ghi `TranMessage` nhật ký), action `InvoiceTemplateController.UpdateBank` + modal "Số tài khoản & ngân hàng trên mẫu hóa đơn" + hiển thị ngân hàng ở `Views/InvoiceTemplate/Index.cshtml`, endpoint minimal-API `/api/templates/{id}/bank` (POST), seed số tài khoản/ngân hàng demo cho mẫu 1C26TAA, 4 test mới. Build Release 0 error 0 warning, 128/128 test pass.
- 2026-06-12 | feat: Cập nhật lại CẢ dải số (số bắt đầu + số kết thúc) của mẫu hóa đơn đang chờ (Invoice_TempInvoice_UpdQtyInvoiceNo) — cho phép điều chỉnh lại toàn bộ dải số được cấp phát của một mẫu hóa đơn đang ở trạng thái chờ (PENDING), khác với thao tác "Tăng số cuối" chỉ mở rộng EndInvoiceNo. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_TempInvoice_UpdQtyInvoiceNo`/`Invoice_TempInvoice_UpdQtyInvoiceNoX` — `Invoice_TempInvoice_CheckDB` chỉ nhận mẫu `TInvoiceStatus=PENDING` + `FlagActive=Active`; chặn khi `(nStartInvoiceNo == 0 || nEndInvoiceNo == 0 || (nEndInvoiceNo - nStartInvoiceNo < 0))` (`..._InvalidValue`); 2 check SQL: `(t.EndInvoiceNo - t.StartInvoiceNo) < t.QtyUsed` và `IsNull(Convert(int, t.LastInvoiceNo), 0) > t.EndInvoiceNo`; update `StartInvoiceNo`/`EndInvoiceNo`/`LUDTimeUTC`/`LUBy`/`UpdQtyInvoiceNoDTimeUTC`/`UpdQtyInvoiceNoBy`/`Remark`; hằng số `TConst.TInvoiceStatus.Pending`/`TConst.Flag.Active` trong `Const.Main.BE.cs`). Thêm enum `TemplateRangeAction.UpdateQtyNo`, trường `OldStartInvoiceNo`/`NewStartInvoiceNo` trên `TemplateRangeLog`, service `UpdateTemplateQtyNoAsync` (chặn mẫu không ở trạng thái chờ, chặn mẫu đã ngừng, chặn dải số không hợp lệ, chặn dải mới nhỏ hơn số đã dùng, chặn số cuối đã cấp vượt số kết thúc mới; cập nhật StartInvoiceNo/EndInvoiceNo + ghi `TemplateRangeLog` + `TranMessage`), action `InvoiceTemplateController.UpdateQtyNo` + modal "Cập nhật dải số" (chỉ hiện khi mẫu Draft) + cập nhật bảng nhật ký dải số hiển thị cả số bắt đầu ở `Views/InvoiceTemplate/Index.cshtml`, helper `Ui.RangeAction` thêm nhãn "Cập nhật dải số", endpoint minimal-API `/api/templates/{id}/update-qty-no` (POST), seed 1 nhật ký cập nhật dải số demo (1..500 → 1..800), 6 test mới. Build Release 0 error 0 warning, 134/134 test pass.
- 2026-06-12 | feat: Nhóm mẫu hóa đơn (Invoice_TempGroup) — mỗi nhóm mẫu gắn với một NNT (MST), định nghĩa thân mẫu hóa đơn (HTML InvoiceTGroupBody) + loại thuế suất (VATType 1VAT/NVAT) + loại hàng hóa/serial (Spec_Prd_Type SPEC/PRODUCTID) + danh sách trường động (Invoice_TempGroupField: DBFieldName/TCFType); hỗ trợ tạo mới/cập nhật theo mã (khóa nghiệp vụ OrgId+InvoiceTGroupCode) và xóa (cascade trường động). Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_TempGroup_Create_New20190706`/`Invoice_TempGroup_Update_New20190706`/`Invoice_TempGroup_Delete_New20190706`/`Invoice_TempGroup_Get_New20190706` + `WAS_*`; `idN.TVAN.Biz/zTemp.cs`: `Invoice_TempGroup_CheckDB` — chặn thiếu mã nhóm, chặn MST không tồn tại, chặn thiếu VATType/tên nhóm, chặn Spec_Prd_Type đồng thời là Spec và ProductId; model `idN.TVAN.Common/Models/Invoice_TempGroup.cs` + `Invoice_TempGroupField.cs`; hằng số `TConst.Spec_Prd_Type` (SPEC/PRODUCTID) trong `Const.Main.BE.cs` và `TConst.Client_VATType` (1VAT/NVAT) trong `Const.Main.cs`; bảng `Invoice_TempGroup`/`Invoice_TempGroupField`). Thêm enum `VATType` (OneVat/NVat) + `SpecPrdType` (Spec/ProductId), entity `InvoiceTempGroup` + `InvoiceTempGroupField`, service `TempGroupsAsync`/`GetTempGroupAsync`/`SaveTempGroupAsync` (lưu lần đầu = tạo, lưu lại = cập nhật; chặn thiếu mã/tên/MST, chặn MST không có NNT; thay thế toàn bộ danh sách trường động)/`DeleteTempGroupAsync`, `TempGroupController` + `Views/TempGroup/Index.cshtml` (bảng nhóm mẫu + lọc theo NNT + form thêm/cập nhật + xóa), helper `Ui.VatType`/`Ui.SpecPrd`, nav link "Nhóm mẫu hóa đơn", endpoint minimal-API `/api/temp-groups` (GET/POST) và `/api/temp-groups/{id}` (DELETE), seed 1 nhóm mẫu MAU1VAT + 2 trường động demo, 5 test mới. Build Release 0 error 0 warning, 139/139 test pass.
- 2026-06-12 | feat: Duyệt NHIỀU hóa đơn cùng lúc (Invoice_Invoice_ApprovedMulti) — duyệt hàng loạt danh sách HĐ đang chờ (PENDING) đã có số trong MỘT thao tác: mỗi HĐ chuyển APPROVED, ghi thời điểm & người duyệt (ApprDTimeUTC/ApprBy), ghi nhật ký duyệt cho từng HĐ + 1 nhật ký duyệt hàng loạt để đối soát; nếu có HĐ không hợp lệ thì KHÔNG duyệt HĐ nào (all-or-nothing). Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_ApprovedMultiX` — lặp qua `dtInput_Invoice_Invoice`, mỗi dòng gọi `Invoice_Invoice_CheckDB` (HĐ phải tồn tại), chặn khi `InvoiceNo` rỗng (`Invoice_Invoice_ApprovedMultiX_InvoiceNoIsNotNull`), set `InvoiceStatus='APPROVED'`/`ApprDTimeUTC`/`ApprBy`/`FlagHotfix='1'`/`InvoiceFileSpec`/`InvoiceFilePath`; `WAS_Invoice_Invoice_ApprovedMulti` — nhận `Lst_Invoice_Invoice`; controller `idN.TVAN.Biz.WebDev/Controllers/InvoiceInvoiceController.cs`: `WA_Invoice_Invoice_ApprovedMulti`). Thêm enum `BulkApproveAction` (BulkApprove), entity `BulkApproveLog` (nhật ký duyệt hàng loạt: ApprovedCount/InvoiceNos/Note/By), service `BulkApproveAsync` (chặn danh sách rỗng, chặn HĐ không tồn tại, chặn HĐ không ở trạng thái chờ, chặn HĐ thiếu số; kiểm tra toàn bộ trước khi ghi rồi duyệt tất cả + ghi `ApproveLog` từng HĐ + `BulkApproveLog` + `TranMessage`)/`BulkApproveLogsAsync`, action `InvoiceController.BulkApprove` + form "Duyệt nhiều hóa đơn cùng lúc" (checkbox chọn HĐ chờ đã có số) ở `Views/Invoice/Index.cshtml`, `BulkApproveLogController` + `Views/BulkApproveLog/Index.cshtml`, nav link "Nhật ký duyệt nhiều HĐ", endpoint minimal-API `/api/invoices/bulk-approve` (POST) và `/api/bulk-approve-logs` (GET), seed 1 nhật ký duyệt hàng loạt demo, 4 test mới. Build Release 0 error 0 warning, 143/143 test pass.
- 2026-06-12 | feat: Bảng tổng hợp hóa đơn (BTH) — màn đối chiếu liệt kê hóa đơn đã phát hành (ISSUED) hoặc đã hủy (DELETED) trong một kỳ (ngày/tháng/quý/năm) kèm trạng thái TThai (Mới/Huỷ/Điều chỉnh/Thay thế) suy ra từ SourceInvoiceCode + InvoiceStatus, và thông tin hóa đơn gốc bị điều chỉnh/thay thế (ký hiệu/mẫu số/số HĐ gốc). Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Invoice.cs`: `Invoice_Invoice_BTHGet`/`Invoice_Invoice_BTHGetX` — lọc `InvoiceStatus in ('ISSUED','DELETED')` theo khoảng ngày `InvoiceDateUTC`, cột `TThai` = case theo `SourceInvoiceCode`+`InvoiceStatus` (ROOT+ISSUED→'0' Mới, ROOT+DELETED→'1' Huỷ, REPLACE+ISSUED→'3' Thay thế, REPLACE+DELETED→'1', ADJ+ISSUED→'2' Điều chỉnh, ADJ+DELETED→'1'), left join `Invoice_Invoice ii_ref on ii.RefNo = ii.InvoiceCode` để lấy ký hiệu/mẫu số/số HĐ gốc; controller `idN.TVAN.Web/Controllers/Mst_GuiTongHopController.cs`: `Invoice_Invoice_BTH` + `WA_Invoice_Invoice_BTHGet` — tính khoảng ngày theo LKDLieu N/T/Q; model `idN.TVAN.Common/Models/TDiepTCT/InvoiceGTH.cs`). Trước đó entity `InvoiceGthRow` + enum `TThai` + service `BthRowsAsync` đã có sẵn trong Models/Service nhưng CHƯA được expose (không controller/view/endpoint/nav). Thêm helper `Ui.TThai`, `BthController.Index` (lọc theo loại kỳ + kỳ dữ liệu) + `Views/Bth/Index.cshtml` (bảng hóa đơn + badge trạng thái + cột HĐ gốc + dòng tổng cộng + form chọn kỳ), nav link "Bảng tổng hợp hóa đơn", endpoint minimal-API `/api/bth` (GET, tham số lkdlieu/kdlieu), 4 test mới (ISSUED→Mới, DELETED→Huỷ, điều chỉnh/thay thế→đúng trạng thái + có HĐ gốc, kỳ rỗng→rỗng). Build Release 0 error 0 warning, 147/147 test pass.
- 2026-06-12 | feat: Danh mục khách hàng / người mua (Mst_CustomerNNT) — mỗi NNT (bên bán) quản lý danh sách khách hàng của mình để chọn nhanh khi lập hóa đơn: tạo mới/cập nhật theo khóa nghiệp vụ (MST, CustomerNNTCode), bật/ngừng dùng (FlagActive) và xóa. Port từ nguồn `2018.5.TVAN` (`idN.TVAN.Biz/Master.cs`: `Mst_CustomerNNT_Get`/`Mst_CustomerNNT_Create`/`Mst_CustomerNNT_Update`/`Mst_CustomerNNT_Delete` + `Mst_CustomerNNT_CheckDB`/`Mst_CustomerNNT_CheckMST` — chặn thiếu CustomerNNTCode, chặn trùng mã khách hàng trong phạm vi NNT, chặn trùng CustomerMST trong phạm vi NNT, MST phải là NNT đã tồn tại; model `idN.TVAN.Common/Models/Mst_CustomerNNT.cs`). Thêm entity `CustomerNnt` (MST/CustomerNNTCode/CustomerNNTName/CustomerMST/Type/Address/Email/Phone/Fax/Contact*/DOB/ProvinceCode/DistrictCode/AccNo/BankName/GovIDType/GovID/Remark/FlagActive/UpdatedAt/UpdatedBy), service `CustomerNntsAsync`/`GetCustomerNntAsync`/`SaveCustomerNntAsync` (lưu lần đầu = tạo, lưu lại = cập nhật; chặn thiếu mã/tên, chặn MST không có NNT, chặn trùng mã khách hàng & trùng MST khách hàng trong phạm vi NNT)/`DeleteCustomerNntAsync`, `CustomerController` + `Views/Customer/Index.cshtml` (bảng khách hàng + lọc theo NNT + form thêm/cập nhật + xóa), nav link "Danh mục khách hàng", endpoint minimal-API `/api/customers` (GET/POST) và `/api/customers/{id}` (DELETE), seed 2 khách hàng demo cho NNT 0101243150, 5 test mới. Build Release 0 error 0 warning, 152/152 test pass.
